---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: openstackblockstorages.openstack.ayr.ca
spec:
  group: openstack.ayr.ca
  names:
    kind: OpenStackBlockStorage
    listKind: OpenStackBlockStorageList
    plural: openstackblockstorages
    singular: openstackblockstorage
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenStackBlockStorage is the Schema for the openstackblockstorages
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenStackBlockStorageSpec defines the desired state
            properties:
              cloud:
                description: Cloud is the name of the cloud in clouds.yaml to use
                  for authentication.
                type: string
              cloudConfig:
                description: CloudConfig is a reference to the secret containing the
                  clouds.yaml file.
                properties:
                  key:
                    description: The key of the secret to select from.  Must be a
                      valid secret key.
                    type: string
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  optional:
                    description: Specify whether the Secret or its key must be defined
                    type: boolean
                required:
                - key
                type: object
                x-kubernetes-map-type: atomic
              projectName:
                description: ProjectName is an optional OpenStack project to query
                type: string
            required:
            - cloud
            - cloudConfig
            type: object
          status:
            description: OpenStackBlockStorageStatus defines the observed state
            properties:
              volumes:
                description: Volumes lists block storage volumes discovered in OpenStack
                items:
                  description: VolumeStatus describes a single volume from OpenStack
                  properties:
                    id:
                      type: string
                    name:
                      type: string
                    sizeGB:
                      type: integer
                    status:
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

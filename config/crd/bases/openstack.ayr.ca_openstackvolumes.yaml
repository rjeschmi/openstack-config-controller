---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: openstackvolumes.openstack.ayr.ca
spec:
  group: openstack.ayr.ca
  names:
    kind: OpenStackVolume
    listKind: OpenStackVolumeList
    plural: openstackvolumes
    shortNames:
    - osv
    singular: openstackvolume
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenStackVolume is the Schema for the openstackvolumes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenStackVolumeSpec defines the desired state of OpenStackVolume
            properties:
              availability_zone:
                description: AvailabilityZone is the availability zone of the volume.
                type: string
              bootable:
                description: Bootable is whether the volume is bootable.
                type: string
              created_at:
                description: CreatedAt is the date and time the volume was created.
                format: date-time
                type: string
              encrypted:
                description: Encrypted is whether the volume is encrypted.
                type: boolean
              id:
                description: ID is the UUID of the volume.
                type: string
              name:
                description: Name is the name of the volume.
                type: string
              size:
                description: Size is the size of the volume in GB.
                type: integer
              status:
                description: Status is the status of the volume.
                type: string
              volume_type:
                description: VolumeType is the type of the volume.
                type: string
            required:
            - availability_zone
            - bootable
            - created_at
            - encrypted
            - id
            - name
            - size
            - status
            - volume_type
            type: object
          status:
            description: OpenStackVolumeStatus defines the observed state of OpenStackVolume
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
